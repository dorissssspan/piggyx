/* Piggy X · Linux 学习心得 */
const LINUX_ARTICLES = [
  {
    id: "linux-20",
    title: "Linux 新手村：面试够用的命令清单",
    date: "2026-08-24",
    tag: "Linux",
    tagClass: "t-linux",
    summary: "不贪多，只留面试和日常最高频的命令：文件操作、权限、进程、日志、网络排查，每条都配一个真实使用场景。",
    content: "\n<p>学 Linux 最容易犯的错是\"背命令大全\"。我的方法相反：只保留<b>面试会问 + 上手就用</b>的那部分，每条命令绑定一个场景记忆。</p>\n<h3>文件与目录：每天都要用</h3>\n<pre># 看看我在哪、有什么\npwd && ls -lh\n\n# 找大文件（磁盘报警时第一步）\ndu -sh /var/log/* | sort -rh | head\n\n# 实时盯着日志新增内容\ntail -f /var/log/syslog</pre>\n<p>面试常问 <code class=\"inline\">ls -l</code> 输出第一列 <code class=\"inline\">-rwxr-xr--</code> 什么意思——答\"文件类型 + 属主/属组/其他人各自的读写执行权限\"，再补一句\"8 进制表示就是 754\"，基本就稳了。</p>\n<h3>权限三件套：chmod / chown / chgrp</h3>\n<pre># 给脚本加可执行权限\nchmod +x deploy.sh\n\n# 递归修改目录属主\nchown -R piggy:piggy /home/piggy/project</pre>\n<blockquote>场景记忆法：部署脚本提示 Permission denied，第一反应 <code class=\"inline\">ls -l</code> 看权限，缺 x 就 chmod +x。</blockquote>\n<h3>进程与资源：服务器卡了先跑这几条</h3>\n<pre>top              # 看 CPU / 内存，按 P 按 M 排序\nps -ef | grep nginx   # 确认进程在不在\nkill -15 1234    # 优雅结束；-9 是强制（最后手段）\nfree -h          # 内存还剩多少\ndf -h            # 磁盘还剩多少</pre>\n<p>面试高频：<b>kill -15 和 -9 的区别？</b>——15 是 SIGTERM，进程可以清理资源后退出；9 是 SIGKILL，内核直接终结，进程来不及收尾，可能丢数据。</p>\n<h3>网络排查：支持岗的看家本领</h3>\n<pre>ping baidu.com            # 通不通\nip a                      # 看本机网卡和 IP\nss -tlnp                  # 端口在不在监听（新工具，netstat 的替身）\ncurl -I http://localhost  # 服务有没有响应</pre>\n<p>经典场景题：\"用户说网站打不开，你怎么查？\"——按<b>链路顺序</b>答：ping 通吗 → 端口监听吗 → 服务进程活着吗 → 日志报什么错。有条理比背命令加分。</p>\n<h3>文本三剑客（入门够用版）</h3>\n<pre>grep -i error app.log     # 在日志里找错误（-i 忽略大小写）\ngrep -c error app.log     # 数一下错了多少次\ncat access.log | awk '{print $1}' | sort | uniq -c | sort -rn | head\n# 统计访问最多的前 10 个 IP —— 面试真爱考这条管道</pre>\n"
  },
  {
    id: "tcp-dns",
    title: "TCP四层+DNS完整流程",
    date: "2026-09-03",
    tag: "Linux",
    tagClass: "t-linux",
    summary: "一般面试官爱问“一个网页请求从输入网址到显示，经历了什么”",
    content: "<h4>TCP/IP四层模型（面试必考）</h4>\n<p>一般面试官爱问“一个网页请求从输入网址到显示，经历了什么”</p>\n<table>\n<thead>\n<tr>\n<th>层</th>\n<th>名字</th>\n<th>作用</th>\n<th>代表协议</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>4</td>\n<td>应用层</td>\n<td>你用的软件</td>\n<td>HTTP / SSH / DNS</td>\n</tr>\n<tr>\n<td>3</td>\n<td>传输层</td>\n<td>数据“快递” —端口号</td>\n<td>TCP / UDP</td>\n</tr>\n<tr>\n<td>2</td>\n<td>网络层</td>\n<td>找路 —— IP地址</td>\n<td>IP / ICMP</td>\n</tr>\n<tr>\n<td>1</td>\n<td>网络接口层</td>\n<td>物理传输——网卡/网线</td>\n<td>以太网</td>\n</tr>\n</tbody>\n</table>\n<p><span style=\"color: red\"><strong>记忆口诀：</strong>“应用选快递（TCP/UDP），网络层找路（IP）,接口层铺路(网卡)。”</span></p>\n<p>对应关系：</p>\n<ul>\n<li>IP地址 = 门牌号 （找到哪台电脑）</li>\n<li>端口号 = 房间号 （找到那个电脑上的哪个程序）</li>\n<li>例： 访问网页 = 找ip门牌 + 敲 80/443房门的门</li>\n</ul>\n<h4>三次握手</h4>\n<p>你  —— SYN(我想连你) →→  服务器\n你  ←← SYN+ACK(好的，我也想你) —— 服务器\n你 —— ACK(收到，开聊)  →→ 服务器</p>\n<p><span style=\"color: red\"><strong>记忆口诀：</strong> “你好→你也好→好，开始”。面试还会问 四次挥手（断开连接）： “再见 → 再见 → 好 → 好”，多一次是因为要等数据传完。</span></p>\n<h4>TCP vs UDP对比（常考）</h4>\n<table>\n<thead>\n<tr>\n<th></th>\n<th>TCP</th>\n<th>UDP</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>连接</td>\n<td>先握手</td>\n<td>不握手直接发</td>\n</tr>\n<tr>\n<td>可靠性</td>\n<td>可靠，丢包重传</td>\n<td>不可靠，丢了不管</td>\n</tr>\n<tr>\n<td>速度</td>\n<td>慢</td>\n<td>快</td>\n</tr>\n<tr>\n<td>典型应用</td>\n<td>HTTP / SSH /MySQL</td>\n<td>DNS / 视频 /语音</td>\n</tr>\n</tbody>\n</table>\n<p><strong>DNS 用 UDP（快，丢一个包重问一次就行）</strong>，常考提。</p>"
  },
  {
    id: "linux-permission",
    title: "Linux：文件权限（面试必考）",
    date: "2026-09-03",
    tag: "Linux",
    tagClass: "t-linux",
    summary: "cd ~/linux-practice/day2 touch test.sh ls -l test.sh #输出 ：-rw-r--r-- 1 dorisvov …",
    content: "<h4>ls -l 输出的 “权限位”</h4>\n<pre><code class=\"language-linux\">cd ~/linux-practice/day2\ntouch test.sh\nls -l test.sh\n#输出 ：-rw-r--r-- 1 dorisvov dorisvov 0 日期 test.sh\n</code></pre>\n<p>拆解：</p>\n<table>\n<thead>\n<tr>\n<th>部分</th>\n<th>含义</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>-</td>\n<td>文件类型（-=普通文件 d=目录 1=软链接）</td>\n</tr>\n<tr>\n<td>rw-</td>\n<td>所有者（U）权限：可读可写</td>\n</tr>\n<tr>\n<td>r--</td>\n<td>所属组（g）权限：只读</td>\n</tr>\n<tr>\n<td>r--</td>\n<td>其他人（o）权限：只读</td>\n</tr>\n<tr>\n<td>dorisvov dorisvov</td>\n<td>所有者 = 所属组</td>\n</tr>\n</tbody>\n</table>\n<p>每个权限三个字母： r = 读（read） w =写（write）x=执行（execute）。</p>\n<p><span style=\"color: red\"><strong>记忆口诀</strong>：“rwx三兄弟，读写在前面，执行最后面”</span></p>\n<h4>数字法改权限（chmod，必考）</h4>\n<p>权限可以翻译成数字： <strong>r=4，w=2，x=1</strong>，加起来：权限rwx = 数字7，rw- = 6，r-x = 5，r-- = 4，--- = 0</p>\n<pre><code class=\"language-linux\">chmod 755 test.sh    # 所有者rwx，组r-x，其他人r-x（网站/脚本最常见的权限）\nchmod 644 test.sh    # 所有者rw-，组r--，其他人r--（普通文件默认）\nchmod 600 test.sh    # 只有自己能读写（密钥文件必用！）\n</code></pre>\n<p><span style=\"color: red\"><strong>记忆口诀</strong>：\"755 网页跑，644 文件躺，600 密钥藏。\"（对应：脚本/目录 755，普通文件 644，私密文件 600）</span></p>\n<h5>字母法（面试爱问区别）</h5>\n<pre><code class=\"language-linux\">chmod u+x test.sh    # 给所有者加执行权限（u=user g=group o=other a=all）\nchmod g-w test.sh    # 给组去掉写权限\nchmod a+r test.sh    # 所有人加读权限\n</code></pre>\n<p><strong>chmod +x</strong> 和 <strong>chmod 755</strong>的区别 —— <code>chomod +x</code> 只加<strong>执行位、不动其他权限</strong>； <code>chmod 755</code> 是<strong>直接指定完整权限</strong>。改脚本要执行用 <code>chmod +x</code>  就够了。</p>\n<h5>目录的x权限</h5>\n<ul>\n<li>文件的 x = 能执行它</li>\n<li>目录的 x = 能进去 （cd进去/访问里面的文件）</li>\n</ul>\n<p>面试经典题：一个目录权限是 r--，你能列出文件名，但打不开里面任何文件（没有 x 进不去）。</p>\n<h4>改所有者：chown</h4>\n<pre><code class=\"language-linux\">sudo chown dorisvov:www-data test.sh   # 改所有者为dorisvov，组为www-data\n</code></pre>\n<p>面试点：web 部署时文件属于 root，网页 403 打不开 → 常见解法 <code>chown</code> 给 web 用户或 <code>chmod 755</code>。</p>\n<h3>练习</h3>\n<pre><code class=\"language-linux\">cd ~/linux-practice/day2\ntouch a.sh b.sh\nchmod 755 a.sh\nchmod 600 b.sh\nls -l a.sh b.sh\n-rwxr-xr-x 1 dorisvov dorisvov 0 Sep  2 00:30 a.sh                                       -rw------- 1 dorisvov dorisvov 0 Sep  2 00:30 b.sh\n</code></pre>\n<ul>\n<li>贴 <code>ls -l</code> 的输出，说出 a.sh 和 b.sh 各自权限数字是多少</li>\n</ul>\n<p>答：a.sh权限数字是755，b.sh权限数字是600</p>\n<p><strong>关键认知：权限数字永远是三位，一位对应一组</strong>（所有者/组/其他人），不是把所有权限加起来变成一个数。你是把\"6\"当成了一个数——不对，6只是第一位的值，后面还有两个0</p>\n<ul>\n<li>额外挑战：<code>chmod u+x a.sh</code> 后再 <code>ls -l</code>，权限变成什么了？</li>\n</ul>\n<p>答：a.sh 现在所有者已经是 <code>rwx</code>（<strong>本来就有 x</strong>），所以再 <code>u+x</code> <strong>什么都没变</strong>，还是 <code>-rwxr-xr-x</code>（755）</p>\n<ul>\n<li>思考题：为什么 SSH 私钥文件（id_rsa）要求权限必须是 600？（提示：别人可读会怎样）</li>\n</ul>\n<p>答：600权限只有自己能读写，避免私钥泄漏；</p>\n<p>方向对，但补充一个面试关键点：<strong>SSH 私钥权限太宽松时，ssh 会直接拒绝使用</strong>，报错 <code>UNPROTECTED PRIVATE KEY FILE</code>——所以 600 不只是\"防止泄露\"，还是 SSH 自己的强制要求。</p>\n<pre><code>chmod 640 b.sh\nls -l b.sh\n</code></pre>\n<ol>\n<li>如果我执行 <code>chmod +x b.sh</code>，b.sh 会变成什么？</li>\n</ol>\n<p>答：b.sh变成 -rwxr-x--x</p>\n<p>chmod +x 默认给<strong>所有人</strong>（u/g/o）都加 x，rw- r-- ---（640） 每组+x等于-rwxr-x--x（751）</p>"
  },
  {
    id: "linux-process",
    title: "进程管理（必会）",
    date: "2026-09-03",
    tag: "Linux",
    tagClass: "t-linux",
    summary: "怎么查看进程/怎么杀进程，就靠这组命令",
    content: "<p>怎么查看进程/怎么杀进程，就靠这组命令</p>\n<h4>看进程：ps（静态快照）</h4>\n<pre><code>ps aux   #看所有进程（面试标准写法！）\nps  -ef    #另一种写法，效果类似\n</code></pre>\n<p>ps aux输出关键：</p>\n<table>\n<thead>\n<tr>\n<th></th>\n<th>含义</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>USER</td>\n<td>谁启动的</td>\n</tr>\n<tr>\n<td>PID</td>\n<td>进程号（杀进程靠它）</td>\n</tr>\n<tr>\n<td>%CPU / %MEM</td>\n<td>占用百分比</td>\n</tr>\n<tr>\n<td>STAT</td>\n<td>状态（S=睡眠  R=运行  Z=僵尸）</td>\n</tr>\n<tr>\n<td>COMMAND</td>\n<td>什么程序</td>\n</tr>\n</tbody>\n</table>\n<p><span style=\"color: red\"><strong>记忆口诀：</strong>“ps aux没有横杠，ps -ef有横杠，两个都背，面试问哪个答哪个。”</span></p>\n<p>配合 grep 找特定进程</p>\n<pre><code>ps aux | grep python   #找所有python进程\n</code></pre>\n<h4>看动态：top（实时刷新）</h4>\n<pre><code>top\n# 按 q 退出\n# 按 M 按内存排序，按 P 按CPU排序（面试问“怎么让top按内存排”答这个）\n</code></pre>\n<p><strong>ps vs top</strong>区别 —— ps看的是<strong>那一刻</strong>的快照，top是<strong>实时刷新</strong>的动态视图。</p>\n<h4>杀进程：kill</h4>\n<pre><code>kill 1234          # 温柔地请进程退出（发TERM信号）\nkill -9 1234       # 强制杀死（发KILL信号，杀不死也杀）\npkill python       # 按名字杀\n</code></pre>\n<p><strong>kill</strong> 和 <strong>kill -9</strong>区别？</p>\n<ul>\n<li><code>kill</code> = 发TERM信号，让程序自己收拾完再退出（保存数据、关文件）</li>\n<li><code>kill -9</code>= 直接强制杀，程序没机会善后（可能丢数据）</li>\n<li>顺序： <strong>先 kill ， 不行再 kill -9</strong></li>\n</ul>\n<p>⚠️安全警告： 杀进程前一定先 <code>sp aux | grep 名字</code> 确认PID，别乱杀 —— 杀错系统进程（比如PID 1)可能导致系统异常。新手别碰 <strong>kill -9</strong> 系统关键进程。</p>\n<h4>后台运行</h4>\n<pre><code>nohup python script.py &amp;     # 后台跑，关终端也不死\n</code></pre>\n<p><code>&amp;</code>放后台，<code>nohup</code> 防挂断，日志默认存 <code>nohup.out</code>。</p>\n<h3>练习</h3>\n<ul>\n<li>跑 <code>ps aux | head -5</code>，贴输出，指出每一列的 USER/PID/COMMAND</li>\n</ul>\n<pre><code>USER         PID %CPU %MEM    VSZ   RSS TTY      STAT START   TIME COMMAND               root           1  0.0  0.1  22128 13608 ?        Ss   Aug30   0:05 /sbin/init             root           2  0.0  0.0   3180  2208 hvc0     Sl+  Aug30   0:00 /init                 root           9  0.0  0.0   3196  2128 hvc0     Sl+  Aug30   0:00 plan9 --control-socket 7 --log-level 4 --server-fd 8 --pipe-fd 10 --log-truncate     \nroot          61  0.0  0.3  75032 24448 ?        S&lt;s  Aug30   0:06 /usr/lib/systemd/systemd-journald\n</code></pre>\n<ul>\n<li>跑 <code>ps aux | grep bash</code>，说出你看到几个 bash 进程</li>\n</ul>\n<pre><code>dorisvov     389  0.0  0.0   7564  6232 pts/0    Ss   Aug30   0:00 -bash                 dorisvov     475  0.0  0.0   6080  5372 pts/1    S+   Aug30   0:00 -bash                 dorisvov    6318  0.0  0.0   3540  1676 pts/0    R+   02:21   0:00 grep --color=auto bash \n</code></pre>\n<p><strong>知识点（面试常考！）</strong>：<code>ps aux | grep bash</code> 会把 <strong>grep 这个命令自己也匹配进去</strong>（因为命令行里含\"bash\"字样）。正确数法要么排除自己：</p>\n<p><code>ps aux | grep bash | grep -v grep</code>    # -v = 排除含grep的行</p>\n<p>要么用专业命令：<code>pgrep -a bash</code>    # 直接列进程名和PID，不匹配自己</p>\n<ul>\n<li>跑 <code>top -bn1 | head -10</code>，看 CPU 那行，说出 load average 三个数是什么意思（提示：1分钟/5分钟/15分钟负载）       输出：load average: 0.00, 0.00, 0.00</li>\n<li>三个数 = 过去 1分钟 / 5分钟 / 15分钟 的系统<strong>平均活跃进程数</strong>（排队等CPU的进程数）</li>\n<li><code>0.00</code> = 系统<strong>基本空闲</strong>，没活干（你机器闲着，正常）</li>\n<li>面试判断标准：<strong>单核 CPU 时 load 到 1.0 = 满载</strong>；双核到 2.0 = 满载。超过核数 = 过载卡顿</li>\n<li>\n<p>三个数一起看趋势：<code>0.5 1.0 1.5</code> 表示<strong>负载在下降</strong>（最近1分钟比15分钟低）；反过来 <code>1.5 1.0 0.5</code> 表示<strong>负载在上升</strong></p>\n</li>\n<li>\n<p>思考题：为什么先 <code>kill</code> 而不是直接 <code>kill -9</code>？</p>\n</li>\n</ul>\n<p>kill是让程序收拾完再退出能最大程度保存数据，kill -9是强制关闭，程序没机会善后容易丢数据。</p>"
  },
  {
    id: "linux-vim",
    title: "vim编辑器（必会）",
    date: "2026-09-03",
    tag: "Linux",
    tagClass: "t-linux",
    summary: "核心就一句话：vim 有三种模式，新手全栽在不会切模式。",
    content: "<p><strong>核心就一句话：vim 有三种模式，新手全栽在不会切模式。</strong></p>\n<h4>三种模式</h4>\n<p>命令模式 —i→ 插入模式</p>\n<p>命令模式 —:→ 末行模式</p>\n<p>Esc键回到命令模式</p>\n<table>\n<thead>\n<tr>\n<th>模式</th>\n<th>干什么</th>\n<th>怎么进</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>命令模式</td>\n<td>删 / 复制 / 移动 （默认进入）</td>\n<td>打开就是</td>\n</tr>\n<tr>\n<td>插入模式</td>\n<td>打字编辑</td>\n<td>按 i</td>\n</tr>\n<tr>\n<td>末行模式</td>\n<td>保存 / 退出 / 查找</td>\n<td>按 :</td>\n</tr>\n</tbody>\n</table>\n<p><span style=\"color: red\"><strong>记忆口诀：</strong>“i进打字，Esc回命令，冒号去末行。”</span></p>\n<h4>必会操作</h4>\n<table>\n<thead>\n<tr>\n<th>按键</th>\n<th>作用</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>i</td>\n<td>进入插入模式（光标前打字）</td>\n</tr>\n<tr>\n<td>Esc</td>\n<td>回命令模式（新手最大坑：打完字不按Esc直接输命令）</td>\n</tr>\n<tr>\n<td>：wq</td>\n<td>保存并退出（w=write，q=quit）</td>\n</tr>\n<tr>\n<td>：q！</td>\n<td>不保存强制退出（改坏了用它！）</td>\n</tr>\n<tr>\n<td>dd</td>\n<td>删掉当前行</td>\n</tr>\n<tr>\n<td>yy</td>\n<td>复制当前行</td>\n</tr>\n<tr>\n<td>p</td>\n<td>粘贴</td>\n</tr>\n<tr>\n<td>/关键词</td>\n<td>搜索，n跳到下一个</td>\n</tr>\n<tr>\n<td>U</td>\n<td>撤销</td>\n</tr>\n<tr>\n<td>G</td>\n<td>跳到文件末尾</td>\n</tr>\n</tbody>\n</table>\n<p><strong>面试场景题：</strong>改坏了配置文件想不保存退出 → Esc 然后 :q! 。</p>\n<h3>实战演练</h3>\n<pre><code>cd ~/linux-practice/day2\nvim test.sh\n</code></pre>\n<p>进去后：\n1. 按 <code>i</code>，输入几行字（比如 <code>echo hello</code>、<code>echo world</code>）\n2. 按 <code>Esc</code> 回命令模式\n3. 输入 <code>:wq</code> 回车保存退出</p>\n<pre><code>cat test.sh\n</code></pre>\n<ol>\n<li>光标停在某一行，按 <code>dd</code> 删掉它</li>\n<li>按 <code>u</code> 撤销</li>\n<li><code>:wq</code> 退出</li>\n</ol>\n<p>⚠️ <strong>注意</strong>：vim 操作时如果按了什么没反应/屏幕乱跳，先按 <code>Esc</code> 冷静一下，再看自己是不是在正确模式。</p>"
  },
  {
    id: "linux-systemd",
    title: "systemd 管服务 + journalctl 看日志(必会)",
    date: "2026-09-03",
    tag: "Linux",
    tagClass: "t-linux",
    summary: "面试必问\"怎么启动/停止一个服务\"\"怎么看服务日志\"",
    content: "<p>面试必问\"怎么启动/停止一个服务\"\"怎么看服务日志\"</p>\n<h4>systemctl管服务</h4>\n<pre><code>sudo systemctl start ssh      # 启动\nsudo systemctl stop ssh       # 停止\nsudo systemctl restart ssh    # 重启（改完配置必用）\nsudo systemctl status ssh     # 看状态（面试标准答法）\nsudo systemctl enable ssh     # 开机自启\n</code></pre>\n<p><span style=\"color: red\"><strong>记忆口诀</strong>：\"start/stop/restart/status/enable 五兄弟，改配置就 restart。\"</span>\n面试点：<strong>restart vs reload 区别</strong>——restart 是彻底重启（断开重连），reload 是只重读配置（不断服务）。改配置用 reload 更优雅：<code>sudo systemctl reload ssh</code></p>\n<h4>status输出怎么看</h4>\n<p><code>systemctl status ssh</code></p>\n<p>关键看两行：</p>\n<ul>\n<li><code>Active: active (running)</code> = 正常运行 ✅</li>\n<li><code>Active: failed</code> = 挂了，得查日志 🔴</li>\n</ul>\n<h4>journalctl看日志</h4>\n<pre><code>journalctl -u ssh              # 看ssh服务的日志\njournalctl -u ssh -n 50        # 只看最近50行（-n=number）\njournalctl -u ssh -f           # 实时滚动看（-f=follow，像top一样）\njournalctl -u ssh --since &quot;1 hour ago&quot;   # 看1小时内的\n</code></pre>\n<p><span style=\"color: red\"><strong>记忆口诀</strong>：\"journalctl -u 服务名，加 -n 看尾巴，加 -f 实时跟。\"</span></p>\n<p>面试场景题：“服务起不来，你怎么排查？” 三步走： systemctl管服务、journalctl看日志、status看状态</p>\n<ol>\n<li><code>systemctl status服务名</code> → 看是不是failed</li>\n<li><code>journalctl -u 服务名 -n 50</code> → 看最后50行报错</li>\n<li>根据报错修配置 → <code>systemctl restart</code> → 再看status</li>\n</ol>"
  },
  {
    id: "linux-ssh-scp-apt",
    title: "ssh/scp + apt +磁盘",
    date: "2026-09-03",
    tag: "Linux",
    tagClass: "t-linux",
    summary: "ssh 用户名@IP地址 # 连远程服务器 ssh -p 2222 用户名@IP # 指定端口（默认22） 面试点：ssh 默认端口 22，-p 指定端口。你在…",
    content: "<h3>ssh远程连接</h3>\n<pre><code>ssh 用户名@IP地址          # 连远程服务器\nssh -p 2222 用户名@IP      # 指定端口（默认22）\n</code></pre>\n<p><strong>面试点</strong>：ssh 默认端口 22，<code>-p</code> 指定端口。你在自己机器上可以连自己测试：<code>ssh dorisvov@127.0.0.1</code></p>\n<p>第一次连会问 <code>Are you sure you want to continue connecting (yes/no)?</code> → 输 <code>yes</code>（记住主机指纹）。然后输密码登录，<code>exit</code> 退出。</p>\n<h3>scp传文件</h3>\n<pre><code>scp 本地文件 用户名@IP:远程路径      # 上传\nscp -r 本地目录 用户名@IP:远程路径   # 传文件夹加 -r\nscp 用户名@IP:远程文件 本地路径      # 下载\n</code></pre>\n<p><span style=\"color: red\"><strong>记忆口诀</strong>：\"scp 跟 cp 一样两个参数，左边源右边目标，只是目标/源变成了 <code>用户@主机:路径</code>。\"</span></p>\n<p>实操 <code>scp ~/linux-practice/day2/test.sh dorisvov@127.0.0.1:~/</code>  传完 <code>ls ~/test.sh</code> 看文件到没到。</p>\n<h3>apt软件包管理</h3>\n<pre><code>sudo apt update           # 更新软件源列表（先跑这个！）\nsudo apt install vim      # 安装软件\nsudo apt remove vim       # 卸载\nsudo apt upgrade          # 升级所有已装软件\n</code></pre>\n<p><code>apt update</code> VS <code>apt upgrade</code> 区别？  </p>\n<ul>\n<li>update = 只刷新“有哪些软件、什么版本”的<strong>清单</strong></li>\n<li>upgrade =  真正<strong>下载安装</strong>更新</li>\n</ul>\n<p><span style=\"color: red\"><strong>记忆口诀：</strong>update看菜单，upgrade才点菜</span></p>\n<h3>磁盘查看</h3>\n<pre><code>df -h          # 看整个磁盘空间（-h=human人类可读）\ndu -sh 目录    # 看某个目录占多大（-s=汇总 -h=可读）\n</code></pre>\n<p>面试必问： df vs du 区别？</p>\n<ul>\n<li>df = 看磁盘还剩多少（整块盘）</li>\n<li>du = 看目录/文件占了多少</li>\n</ul>\n<p><span style=\"color: red\"><strong>记忆口诀：</strong>df管盘，du管目录</span></p>\n<p>日志把磁盘撑爆。正确答案三步走：</p>\n<pre><code>journalctl --disk-usage              # 1. 先看日志占多少\njournalctl --vacuum-size=200M        # 2. 清理到 200M 以下\njournalctl --vacuum-time=7d          # 3. 或只保留 7 天\n</code></pre>\n<blockquote>\n<p>⚠️ <strong>别直接 <code>rm</code> journal 文件</strong>——systemd 正在用它们，正确姿势是 <code>journalctl --vacuum</code>（真空吸尘器 🧹）。想永久限制，改 <code>/etc/systemd/journald.conf</code> 里的</p>\n</blockquote>\n<p><code>SystemMaxUse=500M</code>。</p>"
  }
];
