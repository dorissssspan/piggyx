/* Piggy X · 日常分享 / 读书笔记 */
const READING_ARTICLES = [
  {
    id: "network-how-it-connects-ch1-3",
    title: "《网络是怎样连接的》第 1—3 章笔记",
    date: "2026-09-17",
    tag: "读书笔记",
    tagClass: "t-reading",
    summary: "从输入网址到包发出去：URL 解析与 HTTP 请求响应、DNS 与 Socket 四阶段；协议栈分层、TCP 三次握手/序号 ACK/滑动窗口、UDP 取舍；集线器、交换机 MAC 表与路由器路由表、NAT/DHCP/防火墙。附原书重点页码。",
    content: "<p>这本书真正适合的不是“只想记考试知识点的人”，而是“想把知识点串起来的人”。以后看到 HTTP、TCP、IP、MAC、ARP、DNS、NAT、路由、交换机、PPP、BGP、IX、CDN，别再把它们当成一堆孤立名词——这本书真正训练的是：<strong>看到一个网络现象，你能沿着数据流把背后的整条链路还原出来。</strong></p>\n<p><img alt=\"全书总览：从客户端浏览器经接入网、网络运营商到服务器端的完整链路\" src=\"img/net-overview.png\" /></p>\n<p class=\"img-note\">💡 一张图看懂全流程：第 1 章在浏览器内部，第 2 章在协议栈与网卡，第 3 章在集线器 / 交换机 / 路由器，第 4—6 章在运营商与服务器侧。</p>\n<h2>第一章 浏览器生成消息 —— 探索浏览器内部</h2>\n<p>URL 不只是一个“网址”。例如 <code>https://www.baidu.com/?id=101470</code>，浏览器首先不是马上联网，而是先解析 URL：</p>\n<ul>\n<li><code>http</code>：告诉浏览器采用什么通信规则；</li>\n<li><code>www.baidu.com</code>：目标服务器的名字；</li>\n<li><code>?id=101470</code>：我要它上面的什么东西。</li>\n</ul>\n<p>URL 本质上是在告诉浏览器：<strong>“用什么办法，到哪里，拿什么”</strong>。</p>\n<h3>HTTP 的本质就是“请求 + 响应”</h3>\n<p>浏览器做的事情本质上非常朴素：浏览器（我要这个东西）↔ 服务器（给你），也就是 Request 请求 → Response 响应。HTTP 请求消息里主要有：方法、URI、HTTP 版本、请求头、必要时的消息体。</p>\n<ul>\n<li>GET 本质：<strong>请把这个 URI 对应的数据给我。</strong></li>\n<li>POST 本质：<strong>我还有一些数据要提交给你。</strong></li>\n</ul>\n<p><img alt=\"HTTP 方法一览表：GET、POST、HEAD、OPTIONS、PUT、DELETE、TRACE、CONNECT 在 1.0 / 1.1 中的含义\" src=\"img/net-http-methods.png\" /></p>\n<p class=\"img-note\">💡 考试高频点：PUT、DELETE 在 1.0 里只是规格书附录定义的附加功能，1.1 才正式定义。</p>\n<h3>浏览器不负责真正的网络传输</h3>\n<p>这一点特别容易被忽略，但非常重要：浏览器负责“我要请求什么”，但“怎么把这个请求送出去”不是浏览器亲自干的。它把工作交给操作系统中的<strong>协议栈</strong>——浏览器 → Socket → 协议栈 → 网卡 → 网络。</p>\n<h3>DNS：把“人类名字”换成“机器地址”</h3>\n<p>你看到的是 <code>www.baidu.com</code>，机器通信真正需要的是 <strong>IP 地址</strong>。IP 地址分网络号和主机号两部分，子网掩码用来划分“哪几位是网络号、哪几位是主机号”：主机号全 0 代表整个子网，主机号全 1 代表向子网里所有设备广播。</p>\n<p>于是需要 DNS，大白话就是人们的<strong>电话簿</strong>：你说“我要找张三”，DNS 告诉你“张三的电话是 150xxxx”。</p>\n<h3>Socket 是应用程序与网络之间的接口</h3>\n<p>Socket 可以理解成<strong>应用程序拿来“使用网络”的一个窗口 / 通道</strong>。浏览器拿到 IP 之后，调用 Socket 库里的组件按顺序工作：先 <code>socket</code> 创建套接字，再 <code>connect</code> 连接服务器，然后 <code>write</code> 发送数据，最后 <code>close</code> 断开。</p>\n<p>客户端和服务器各有一个套接字，两个套接字连起来就形成一条“管道”，数据从管道里流过去。整个收发过程四个阶段：<strong>创建套接字 → 连接服务器 → 收发数据 → 断开并删除套接字</strong>。</p>\n<blockquote>大白话：浏览器像坐在办公室里的老板，只负责决定“发什么、发给谁”，跑腿送信交给楼下的收发室（协议栈）。收发室先开一个专用信箱（创建套接字），再打电话跟对方收发室对上通道（连接），把信塞进信箱（发送数据），送完把信箱拆掉（断开）。</blockquote><p><img alt=\"浏览器调用 Socket 库查询 DNS 的完整调用流程（共 10 步）\" src=\"img/net-socket-dns-flow.png\" /></p>\n<p class=\"img-note\">💡 DNS 查询并不神秘：浏览器 → Socket 的 gethostbyname → 协议栈收发 UDP → 网卡 → DNS 服务器，再原路把 IP 带回来。</p>\n\n<p>另一个好记的类比是<strong>去餐厅点餐</strong>：URL = 菜单地址；DNS = 找到菜单上的套餐号码；HTTP = 你说“我要这道菜”；Socket = 你和餐厅联系的窗口；协议栈 = 后勤人员；网卡 = 真正把消息送上路的人。</p>\n<p><em>推荐细看：第 45—54 页，这十页把“四阶段”框架搭起来，是第二章的预告片。</em></p>\n<h2>第二章 用电信号传输 TCP/IP 数据 —— 探索协议栈和网卡</h2>\n<p>第一章解决的是“我要发送什么”，第二章解决的是<strong>“这个东西到底怎样从电脑里变成网络包，并真的发出去？”</strong></p>\n<h3>协议栈是分层工作的</h3><p><img alt=\"数据发送流程五步：应用程序 → TCP/UDP → IP → 以太网 → 网卡\" src=\"img/net-data-flow.jpg\" /></p>\n<p class=\"img-note\">💡 每一层只干一件事：生成数据、端口封装、寻址路由、成帧传输、物理发送。</p>\n\n<ul>\n<li>上层 <strong>TCP/UDP</strong>：负责“数据怎么可靠地送”；</li>\n<li>中间 <strong>IP</strong>：负责“往哪个地址送”；</li>\n<li>下层 <strong>网卡驱动 + 网卡硬件</strong>：负责“把数据变成电信号”。</li>\n</ul>\n<p>套接字到底是什么？它<strong>不是一个实体物件，而是协议栈内部的一块内存空间</strong>，里面记录着这次通信的控制信息：对方的 IP 地址、端口号、通信状态等。协议栈就是根据这块内存里的信息来决定“下一步该干什么”。</p>\n<blockquote>套接字像去快递站寄件时领的那张快递单：单子上写着收件人地址、电话、寄件人信息，快递员每次都看这张单子操作；你手里的单号（描述符）告诉快递员“处理我这张单子”。应用程序就像“快递大户”，提前把地址写成一张张号码牌，以后每次只说“用 1 号牌发”。</blockquote>\n<p><em>推荐细看：第 61—67 页，尤其 63—66 页“套接字的实体就是通信控制信息”，是理解第二章的钥匙。</em></p>\n<h3>TCP 的核心：可靠地运数据</h3>\n<p><strong>连接服务器：</strong>客户端和服务器之间其实只是在互相交换控制信息，确认双方都准备好了，然后各自在内存里记下“我和对方已经连上了”。TCP 头部里最关键的是<strong>端口号</strong>：客户端随机选一个端口，服务器用固定端口（HTTP 是 80），两边一配对，就知道该把数据交给哪个程序。实际过程：客户端“我想连你”→ 服务器“行，我准备好了”→ 客户端“好，那开始吧”，这就是<strong>三次握手</strong>的雏形。</p>\n<p><img alt=\"TCP 三次握手：SYN → SYN+ACK → ACK\" src=\"img/net-tcp-handshake.jpg\" /></p>\n<p class=\"img-note\">💡 记忆口诀：拨号、应答、确认，然后开始说话。TCP 的“连接”不是拉一条专线，而是双方达成状态共识。</p>\n<p><strong>收发数据：</strong>协议栈拿到 HTTP 请求消息后不会立刻发出去，而是先放进<strong>缓冲区</strong>，等积累到一定量再发，避免频繁发小包浪费网络资源。发出去的数据会被<strong>拆分</strong>成一个个小包，每个包都有<strong>序号</strong>；接收方收到后返回 <strong>ACK 号</strong>（确认号）告诉发送方“我收到了第几个包”，发送方据此判断有没有丢包，丢了就重发。</p>\n<h3>滑动窗口：解决“别让双方一直等”</h3>\n<p>傻等 ACK 会浪费时间，所以 TCP 引入<strong>滑动窗口</strong>：发送方不用等一个包确认了再发下一个，而是连续发多个包，边发边等确认；接收方通过“窗口”告诉发送方“我这边还能收多少”，发送方据此调整速度。</p>\n<p><img alt=\"TCP 滑动窗口的送货类比：边发边等确认\" src=\"img/net-tcp-sliding-window.jpg\" /></p>\n<p class=\"img-note\">💡 不必等每车签收再发下一车；接收方仓库满了就把窗口调小，空了就调大——这就是流量控制的本质。</p>\n<p><em>推荐细看：第 75—89 页，其中 79—84 页（ACK 号与窗口管理）是重中之重。</em></p>\n<h3>断开：为什么要四次</h3>\n<p>数据发完之后不能直接把套接字删掉，需要先<strong>断开连接</strong>，确认双方都不再发数据了，才能删除。断开比连接多一步，通常是<strong>四次交换</strong>：一方说“我没数据要发了”，另一方说“知道了”，等另一方也没数据了再说“我也没了”，最后确认——因为 TCP 连接是<strong>双向</strong>的，两个方向要分别关闭。套接字删除后那块内存释放，下次通信重新创建。</p>\n<p><img alt=\"TCP 四次挥手：FIN → ACK → FIN → ACK\" src=\"img/net-tcp-teardown.jpg\" /></p>\n<p class=\"img-note\">💡 断开比连接多一步，因为 TCP 是双向的，两个方向要分别关闭。</p>\n<h3>IP 和以太网负责不同层次的问题</h3>\n<p>TCP 把数据拆成包之后交给 IP 层。IP 层给每个包加上 <strong>IP 头部</strong>，里面写着发送方 IP 和接收方 IP。但光有 IP 地址还不够，数据在局域网里实际传输靠的是 <strong>MAC 地址</strong>。</p>\n<p>怎么由 IP 查到 MAC？靠 <strong>ARP</strong>：IP 层先发一个 ARP 请求“谁是这个 IP？把你的 MAC 地址告诉我”，对应机器回应后，IP 层就知道该把包发给哪个 MAC。包到网卡后，网卡把数字信息<strong>变成电信号或光信号</strong>通过网线 / 光纤发出去；接收端做相反操作，一层层拆包，最终把数据交给协议栈。</p>\n<p><img alt=\"IP 地址、MAC 地址与 ARP 的快递类比\" src=\"img/net-ip-mac-arp.jpg\" /></p>\n<p class=\"img-note\">💡 IP 负责找到网络位置（楼栋门牌），MAC 负责找到最后一公里的设备（房间门牌），ARP 就是“在楼下喊一声谁是 502”。</p>\n<h3>UDP 为什么存在</h3>\n<p>UDP 和 TCP 最大的区别是：<strong>不确认、不重发、不排序</strong>。它只是把数据打包扔出去，不关心对方有没有收到。TCP 适合需要可靠传输的场景（网页、文件下载）；UDP 适合“丢了也没关系、但要快”的场景，比如 DNS 查询、视频直播、语音通话。</p>\n<h2>第三章 从网线到网络设备 —— 探索集线器、交换机和路由器</h2>\n<h3>集线器：我收到，就向所有线路发</h3>\n<p>网卡里的 <strong>PHY 芯片</strong>负责把数字信号转换成电信号，通过双绞线发送。双绞线传输的是<strong>差分信号</strong>——不看单根线的电压，而看两根线之间的电压差。好处是抗噪声能力强：外界干扰会同时影响两根线，电压差基本不变，信号就不容易出错。</p>\n<p>信号在网线里会<strong>衰减</strong>，距离越长越弱。集线器的作用之一是<strong>中继放大信号</strong>。它本质是<strong>多端口中继器</strong>，工作在物理层，不认识 MAC 也不认识 IP，收到一个包就<strong>广播到所有端口</strong>。</p>\n<p>集线器还有一个特点：<strong>半双工</strong>。同一时刻只能有一台设备发送，两台同时发就会<strong>冲突</strong>，冲突后双方随机等待一段时间再重发，这套机制叫 <strong>CSMA/CD</strong>。</p>\n<h3>交换机：认 MAC 地址</h3>\n<p>交换机工作在<strong>数据链路层</strong>，认识的是 MAC 地址，不认识 IP 地址。它内部有一张 <strong>MAC 地址表</strong>，记录“哪个 MAC 地址接在哪个端口上”。这张表不是永久有效的，会定期<strong>老化删除</strong>——因为设备可能换端口、关机、搬走，旧记录不能一直留着。</p><p><img alt=\"交换机收到数据包后的学习与转发过程\" src=\"img/net-switch-mac-table.jpg\" /></p>\n<p class=\"img-note\">💡 先学后转：把源 MAC 和它所在的端口记进 MAC 地址表，再按目标 MAC 查表转发；表里查不到才广播。</p>\n\n<ul>\n<li>支持<strong>全双工</strong>：端口之间可独立通信，A→B 的同时 C→D 互不冲突，所以不需要 CSMA/CD。</li>\n<li>支持<strong>自动协商</strong>速度和双工模式；协商失败可能退化成半双工，性能下降。</li>\n<li>重要局限：<strong>不隔离广播域</strong>，广播包会从所有端口泛洪出去；要隔离广播域需要 VLAN 或路由器。</li>\n</ul>\n<p><em>推荐细看：约第 151—159 页，MAC 地址表如何学习与转发是本章核心。</em></p>\n<h3>路由器：认 IP 地址</h3>\n<p>路由器工作在<strong>网络层</strong>，根据 IP 地址转发包。交换机是在<strong>同一个网络内部</strong>转发，路由器是在<strong>不同网络之间</strong>转发。它的每个端口各有自己的 IP 和 MAC，内部有一张<strong>路由表</strong>，记录：目标网络地址、子网掩码、下一跳地址、输出端口。</p>\n<p>转发过程：看包里的目标 IP → 用子网掩码匹配路由表 → 多条匹配时选<strong>最长匹配</strong> → 一条都匹配不上就走<strong>默认路由</strong> → 转发前把 <strong>TTL 减 1</strong>（TTL 是包的寿命，减到 0 就丢弃，防止包在网络里无限打转）。</p>\n<p>然后路由器通过 <strong>ARP</strong> 找到下一跳的 MAC 地址，把包重新封装成以太网帧。注意这句：<strong>IP 地址不变，MAC 地址会变</strong>——源 MAC 变成路由器出口的 MAC，目标 MAC 变成下一跳的 MAC。路由器还会<strong>隔离广播域</strong>，每个端口是一个独立的广播域。</p>\n<p><img alt=\"交换机、路由器与 TTL 的快递转运类比\" src=\"img/net-router-ttl.jpg\" /></p>\n<p class=\"img-note\">💡 交换机负责小区内转送，路由器决定跨区方向（IP 地址不变、MAC 每站都换），TTL 限制最多转运次数，超了就丢弃。</p>\n<h3>路由器的三个附加功能</h3>\n<ol>\n<li><strong>NAT / NAPT（前台公司）：</strong>家里多台设备但运营商只给一个公网 IP，路由器用 NAT 把私有 IP 转成公网 IP。NAPT 更进一步，用<strong>端口号</strong>区分不同设备的连接：内部 <code>192.168.1.2:12345</code> 出去变成 <code>公网IP:54321</code>，路由器记下映射，回来的包再转回内部设备。</li>\n<li><strong>DHCP（酒店自动分房）：</strong>路由器通常内置 DHCP 服务器，自动分配 IP 地址、子网掩码、默认网关、DNS 服务器。插上网线就能上网，就是它的功劳。</li>\n<li><strong>防火墙 / 包过滤（小区门禁）：</strong>检查进入的包，根据 IP、端口、TCP 控制位等决定放行还是丢弃，挡住一部分外部主动进来的连接。</li>\n</ol>\n<h2>三章串成一条线</h2>\n<p><strong>URL 解析 → 生成 HTTP 请求 → DNS 查 IP → Socket / 协议栈委托 → TCP 拆包 + IP/MAC 封装 → 网卡变电信号 → 交换机（看 MAC）→ 路由器（看 IP、做 NAT）→ 进入互联网。</strong>以后遇到任何网络现象，都可以顺着这条链路倒推回去找原因——这也是这本书最值钱的地方。</p>"
  }
];
