/* Piggy X · MySQL 学习心得 */
const MYSQL_ARTICLES = [
  {
    id: "mysql-create-db",
    title: "建第一个库和表",
    date: "2026-09-04",
    tag: "MySQL",
    tagClass: "t-mysql",
    summary: "Linux系统启动mysql",
    content: "<p>Linux系统启动mysql</p>\n<p><code>sudo mysql</code></p>\n<p><strong>注意</strong>：Ubuntu  的 MySQL，root 默认用 <code>auth_socket</code> 认证——<strong>你的系统用户是 root 就能免密进</strong>。所以 <code>sudo mysql</code> 直接进，不用密码。这是 Ubuntu 专属坑（CentOS 上就是另一套），面试官爱问。</p>\n<pre><code class=\"language-mysql\">SELECT VERSION();        -- 看版本\nSHOW DATABASES;          -- 看现有数据库（mysql/sys/information_schema...）\nSELECT USER(), CURRENT_USER();  -- 看你现在是谁\n</code></pre>\n<p>SQL 命令末尾的 <code>;</code> 是\"执行\"的意思，忘了分号命令就不跑，只换行——这也是个新手坑；</p>\n<p>四个系统库含义</p>\n<table>\n<thead>\n<tr>\n<th style=\"text-align: center;\">库</th>\n<th style=\"text-align: center;\">作用</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td style=\"text-align: center;\">information_schema</td>\n<td style=\"text-align: center;\">元数据字典（所有库/表/字段的\"档案室\"）</td>\n</tr>\n<tr>\n<td style=\"text-align: center;\">mysql</td>\n<td style=\"text-align: center;\">用户、权限表都在这</td>\n</tr>\n<tr>\n<td style=\"text-align: center;\">performance_schema</td>\n<td style=\"text-align: center;\">性能监控数据</td>\n</tr>\n<tr>\n<td style=\"text-align: center;\">sys</td>\n<td style=\"text-align: center;\">给DBA看的便捷视图</td>\n</tr>\n</tbody>\n</table>\n<blockquote>\n<p>USER()  = 你声称是谁（客户端），CURRENT_USER() = MySQL实际认定你是谁（认证后）。</p>\n<p>如果哪天两者不同，说明你通过授权账号连接，认证身份不等于登录身份。</p>\n</blockquote>\n<pre><code class=\"language-sql\">CREATE DATABASE school;          -- 建库\nUSE school;                      -- 切换到 school 库（面试常考！不切就建错地方）\nCREATE TABLE student (\n  id INT PRIMARY KEY AUTO_INCREMENT,   -- 主键+自增：每行自动编号 1,2,3...\n  name VARCHAR(20) NOT NULL,           -- 变长字符串，最长20字符，不许为空\n  age INT,                             -- 整数\n  score DECIMAL(5,2)                   -- 总共5位，小数2位：如 98.50\n);\n</code></pre>\n<ul>\n<li>\n<p><code>PRIMARY KEY</code> = 主键，唯一标识一行，<strong>不能重复</strong></p>\n</li>\n<li>\n<p><code>AUTO_INCREMENT</code> = 自增，插入时不写 id 它自动 +1</p>\n</li>\n</ul>\n<p><strong>永不复用</strong>被删掉的编号。比如删了 id3 号，新数据从 4 开始，不会回去补 3 的空位——这在业务上很重要（订单号/流水号不能重复利用）。</p>\n<ul>\n<li><code>VARCHAR(20)</code> vs <code>INT</code>：字符串 vs 整数</li>\n</ul>\n<p>面试常问 <strong>VARCHAR 和 CHAR 区别——先记住 VARCHAR 变长省空间</strong></p>\n<ul>\n<li>\n<p><code>NOT NULL</code> = 这列不许为空</p>\n</li>\n<li>\n<p><code>DECIMAL(5,2)</code> = 钱的类型用 DECIMAL 不用 FLOAT（浮点会丢精度，面试坑）</p>\n</li>\n</ul>\n<h3>DESC表结构</h3>\n<table>\n<thead>\n<tr>\n<th style=\"text-align: center;\">列</th>\n<th style=\"text-align: center;\">意思</th>\n<th style=\"text-align: center;\">你的表</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td style=\"text-align: center;\">Field</td>\n<td style=\"text-align: center;\">字段名</td>\n<td style=\"text-align: center;\">id / name /age /score</td>\n</tr>\n<tr>\n<td style=\"text-align: center;\">Type</td>\n<td style=\"text-align: center;\">数据类型</td>\n<td style=\"text-align: center;\">int/ varchar(20) / decimal(5,2)</td>\n</tr>\n<tr>\n<td style=\"text-align: center;\">Null</td>\n<td style=\"text-align: center;\">能不能为空</td>\n<td style=\"text-align: center;\">NO=不能，YES=能</td>\n</tr>\n<tr>\n<td style=\"text-align: center;\">Key</td>\n<td style=\"text-align: center;\">键类型</td>\n<td style=\"text-align: center;\">PRI = PRIMARY KEY 主键</td>\n</tr>\n<tr>\n<td style=\"text-align: center;\">Default</td>\n<td style=\"text-align: center;\">默认值</td>\n<td style=\"text-align: center;\">没设就是NULL</td>\n</tr>\n<tr>\n<td style=\"text-align: center;\">Extra</td>\n<td style=\"text-align: center;\">额外属性</td>\n<td style=\"text-align: center;\">auto_increment自增</td>\n</tr>\n</tbody>\n</table>\n<h3>增删改查</h3>\n<h4>增 — INSERT</h4>\n<pre><code class=\"language-sql\">INSERT INTO student (name, age, score) VALUES ('小明', 18, 95.5);\nINSERT INTO student (name, age, score) VALUES ('小红', 19, 88);\nINSERT INTO student (name, age, score) VALUES ('小刚', 17, 72.5);\n</code></pre>\n<p><strong>三个必记点</strong>：\n1. <code>INSERT INTO 表名 (列名...) VALUES (值...)</code>——列和值<strong>一一对应</strong>\n2. <strong>id 不用写</strong>！AUTO_INCREMENT 自动给 1、2、3\n3. <strong>字符串要加引号</strong> <code>'小明'</code>，数字不用——漏引号是最常见报错（<code>Unknown column</code> 就是它）</p>\n<blockquote>\n<p>中文能正常存，靠的是 MySQL 8.0 默认 <code>utf8mb4</code> 字符集。面试题\"中文乱码怎么办\"答案第一句：确认库/表是 utf8mb4。</p>\n</blockquote>\n<h4>查 — WHERE条件过滤</h4>\n<pre><code class=\"language-sql\">SELECT * FROM student WHERE score &gt; 80;      -- 只查 80 分以上的\nSELECT * FROM student ORDER BY score DESC;   -- 按分数从高到低排\n</code></pre>\n<blockquote>\n<p>面试考点：<code>WHERE</code> = 逐行过滤条件；<code>ORDER BY</code> = 排序（DESC 降序/ASC 升序，默认升序）。<code>WHERE score &gt; 80</code> 里的 <code>&gt;</code> 是<strong>比较运算符</strong>，还有 <code>&lt;</code>、<code>&gt;=</code>、<code>=</code>、<code>!=</code>。</p>\n</blockquote>\n<h4>改 — UPDATE —— 重点中的重点</h4>\n<pre><code class=\"language-sql\">UPDATE student SET score = 90 WHERE name = '小刚';\n-- update 表名 set 字段 = 条件1 where 条件2\nUPDATE student SET class = '一班' WHERE id IN (1, 2);  -- IN=在列表里，一次改多行\n</code></pre>\n<p>UPDATE student SET score = 90; —— 忘写 WHERE = <strong>全表所有人的分数都变 90</strong> 同理 <code>DELETE FROM student;</code> 忘 WHERE = <strong>整张表清空，没有后悔药</strong>；</p>\n<h4>删：DELETE</h4>\n<pre><code class=\"language-sql\">DELETE FROM student WHERE id = 3;    -- 只删 id=3 的小刚\n</code></pre>\n<blockquote>\n<p><strong>先 SELECT 再 UPDATE/DELETE</strong>：先跑 <code>SELECT * FROM student WHERE ...</code> 确认选中的正是你要改/删的行，再执行修改。</p>\n</blockquote>\n<h3>练习</h3>\n<pre><code class=\"language-sql\"># 1. 先查：看看 &gt;80 分的有谁（预计小明、小红）\nSELECT * FROM student WHERE score &gt; 80;\n\n# 2. 改：把小刚分数改成 90（先 SELECT 确认，再 UPDATE）\nSELECT * FROM student WHERE name = '小刚';\nUPDATE student SET score = 90 WHERE name = '小刚';\n\n# 3. 删：删掉小刚这行（同理先确认）\nDELETE FROM student WHERE id = 3;\n\n# 4. 最终验证\nSELECT * FROM student;\n</code></pre>"
  },
  {
    id: "mysql-select-advanced",
    title: "SELECT进阶",
    date: "2026-09-04",
    tag: "MySQL",
    tagClass: "t-mysql",
    summary: "ALTER TABLE student ADD COLUMN class VARCHAR(10); -- 加&quot;班级&quot;列 批量插入：一次性6行…",
    content: "<h4>ALTER：添加一列</h4>\n<pre><code class=\"language-sql\">ALTER TABLE student ADD COLUMN class VARCHAR(10);   -- 加&quot;班级&quot;列\n</code></pre>\n<h4>批量插入：一次性6行</h4>\n<pre><code class=\"language-sql\">INSERT INTO student (name, age, score, class) VALUES\n('小芳', 18, 92.5, '一班'),\n('小强', 20, 65,   '一班'),\n('小丽', 19, 78,   '二班'),\n('小军', 18, 85.5, '二班'),\n('小美', 21, 55,   '三班'),\n('小杰', 20, 88,   '三班');\n</code></pre>\n<blockquote>\n<p>知识点：<code>VALUES</code> 后面逗号分隔多组括号 = 一次插多行，比插 6 次快得多。</p>\n</blockquote>\n<h4>WHERE进阶</h4>\n<pre><code class=\"language-sql\">-- ① AND：两个条件都要满足（一班 且 80分以上）\nSELECT * FROM student WHERE score &gt;= 80 AND class = '一班';\n\n-- ② LIKE 模糊查询：% 代表任意长度字符（小X都算）\nSELECT * FROM student WHERE name LIKE '小%';\n\n-- ③ BETWEEN：区间查询（70~90分，含边界）\nSELECT * FROM student WHERE score BETWEEN 70 AND 90;\n\n-- ④ LIMIT：只取前3行（排行榜/分页全靠它）\nSELECT * FROM student ORDER BY score DESC LIMIT 3;\n</code></pre>\n<p><strong>面试坑预警</strong>：\n- <code>AND</code> 优先级比 <code>OR</code> <strong>高</strong>！<code>A OR B AND C</code> 实际是 <code>A OR (B AND C)</code>——想改变顺序必须加括号 <code>(A OR B) AND C</code>\n- <strong>隐式类型转换（implicit type conversion）机制：一个值单独出现在 OR/AND 后面当条件时，MySQL 需要它变成 TRUE/FALSE</strong>\n- <code>LIKE '%小%'</code> = 名字里带\"小\"；<code>%</code> 可以放前后任意位置，<code>_</code> 只匹配<strong>一个</strong>字符\n- <code>LIMIT 3</code> = 前3行；<code>LIMIT 2, 3</code> = 跳过2行取3行（第3~5行），分页公式：<code>LIMIT (页号-1)*每页数, 每页数</code></p>\n<h4>GROUP BY语法</h4>\n<pre><code class=\"language-sql\">SELECT class, COUNT(*) FROM student GROUP BY class; \n--**按班级分组，每组数人数** → 输出&quot;一班 3、二班 3、三班 2&quot;。\n</code></pre>\n<pre><code class=\"language-sql\">-- ① 每班人数\nSELECT class, COUNT(*) FROM student GROUP BY class;\n\n-- ② 每班平均分（AVG 自动处理小数）\nSELECT class, AVG(score) FROM student GROUP BY class;\n\n-- ③ 每班最高分最低分\nSELECT class, MAX(score), MIN(score) FROM student GROUP BY class;\n\n-- ④ 平均分 &gt;= 80 的班（HAVING 登场！）\nSELECT class, AVG(score) AS 平均分 FROM student GROUP BY class HAVING AVG(score) &gt;= 80;\n</code></pre>\n<p>⚠️ <strong>HAVING vs WHERE</strong></p>\n<p><code>SELECT class FROM student WHERE AVG(score) &gt;= 80</code> → <strong>报错！</strong></p>\n<p>聚合函数理解：一台“榨汁机”<strong>吃进去很多行，吐出来一个数</strong></p>\n<p>COUNT(*)  = 数人头（8 个人 → 8）\nAVG(score)= 算平均（8 个分数 → 1 个平均分）\nMAX(score)= 挑最高（8 个分数 → 1 个最高分）</p>\n<p>关键点：<strong>一旦榨成汁，就回不去单个水果了</strong>。AVG 之后，你就再也看不见\"小明 95.5、小红 88\"这些行——它们被压成了一个数字。</p>\n<p>什么时候用HACING?</p>\n<p><strong>口诀</strong>：条件里带\"平均/总共/最高\"这种<strong>算出来的词</strong> → HAVING；只挑原始数据 → WHERE。</p>\n<p>✅ 聚合函数的结果只能放 <strong>HAVING</strong> 里过滤为什么？因为执行顺序是：</p>\n<pre><code>FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT\n①找表  ②筛行   ③分组   ④筛组   ⑤选列   ⑥排序   ⑦截断\n</code></pre>\n<p>WHERE 在③分组<strong>之前</strong>跑，那时还没有\"组平均分\"这个概念；HAVING 在分组<strong>之后</strong>跑，才能筛组。</p>\n<blockquote>\n<p>一句话钩子：<strong>WHERE 筛行，HAVING 筛组</strong>——行用 WHERE，组用 HAVING，顺序错了就报错。\n另外 <code>AS 平均分</code> 是别名（给列起个显示名）</p>\n</blockquote>\n<h4>JOIN多表</h4>\n<p>JOIN 就是数据库版 VLOOKUP（Excel 查表），<strong>ON 条件就是\"拿哪一列去查\"</strong>：<code>ON s.class = c.name</code> = = 拿学生的\"班级\"列，去班级表里对\"班级名\"列</p>\n<p>新建class表</p>\n<pre><code class=\"language-sql\">CREATE TABLE class (\n  name VARCHAR(10) PRIMARY KEY,   -- 班级名，主键\n  teacher VARCHAR(10),            -- 班主任\n  room VARCHAR(10)                -- 教室\n);\n\nINSERT INTO class (name, teacher, room) VALUES\n('一班', '王老师', '101'),\n('二班', '李老师', '102'),\n('三班', '赵老师', '103');\n</code></pre>\n<h4>INNER JOIN：把学生和班级拼起来</h4>\n<pre><code class=\"language-sql\">SELECT s.name, s.score, c.teacher, c.room\nFROM student s\nINNER JOIN class c ON s.class = c.name;\n</code></pre>\n<p><strong>读法</strong>：学生表（别名 <code>s</code>）<strong>内连接</strong>班级表（别名 <code>c</code>），<strong>连接条件</strong> <code>ON s.class = c.name</code>（学生表的班级名 = 班级表的班级名）→ 拼出\"每个学生 + 他班主任和教室\"。</p>\n<blockquote>\n<p><code>s</code>、<code>c</code> 是<strong>表别名</strong>，偷懒缩写，写了之后所有列都要用 <code>s.name</code> 这种带前缀的写法。</p>\n</blockquote>\n<pre><code class=\"language-sql\">-- ① 验证你的 HAVING（预期：一班 4 人出来）\nSELECT class, COUNT(*) AS 人数 FROM student GROUP BY class HAVING COUNT(*) &gt;= 3;\n\n-- ② 给班级表加一个&quot;没人报名的四班&quot;\nINSERT INTO class (name, teacher, room) VALUES ('四班', '钱老师', '104');\n\n-- ③ INNER JOIN 看看：四班会出现在结果里吗？\nSELECT s.name, c.name, c.teacher\nFROM student s\nINNER JOIN class c ON s.class = c.name;\n\n-- ④ LEFT JOIN：换个方向，从班级出发找学生\nSELECT c.name, c.teacher, s.name AS 学生\nFROM class c\nLEFT JOIN student s ON c.name = s.class;\n</code></pre>\n<p><strong>先猜再看</strong>（培养批判眼）：\n- ③ 结果里<strong>不会有四班</strong>——内连接只要\"两边都匹配\"的，四班没学生，被丢弃\n- ④ 结果里<strong>四班会出现</strong>，但\"学生\"列是 <strong>NULL</strong>（空）——这就是 LEFT JOIN 的\"左\"：<strong>左边的表（class）全保留，右边（student）匹配不上就补 NULL</strong></p>\n<blockquote>\n<p>面试经典题：<strong>\"找出没有学生的班级 / 没有订单的客户\"</strong>——答案就是 LEFT JOIN + <code>WHERE s.id IS NULL</code>，这个套路背下来，面试直接默写。</p>\n</blockquote>\n<h3>DELETE vs TRUNCATE vs DROP（面试命中率 90%）</h3>\n<table>\n<thead>\n<tr>\n<th></th>\n<th>DELETE</th>\n<th>TRUNCATE</th>\n<th>DROP</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>删什么</td>\n<td>行（可带where）</td>\n<td>清空整表数据</td>\n<td>整张表都没了</td>\n</tr>\n<tr>\n<td>能不能反悔</td>\n<td>事务内可回滚</td>\n<td>不可回滚</td>\n<td>不可回滚</td>\n</tr>\n<tr>\n<td>自增id</td>\n<td>不重置（接着编号）可以</td>\n<td>重置（从1开始）不行</td>\n<td>表都没了不行</td>\n</tr>\n<tr>\n<td>带where</td>\n<td>可以</td>\n<td>不行</td>\n<td>不行</td>\n</tr>\n<tr>\n<td>本质</td>\n<td>DML（数据操作）</td>\n<td>DDL（结构操作)</td>\n<td>DDL</td>\n</tr>\n</tbody>\n</table>\n<blockquote>\n<p>一句话记忆：<strong>DELETE 删人、TRUNCATE 清场、DROP 拆楼</strong>。面试爱问\"想清空表但保留表结构用哪个\"→ 答 TRUNCATE（但不可回滚，慎用）。</p>\n</blockquote>\n<p><strong>总结</strong></p>\n<pre><code>INNER JOIN = 交集（两边都有才留）,\nLEFT JOIN  = 左表全留 + 右边补 NULL\nON         = 连接条件（两表怎么对上）\n找&quot;孤儿/空&quot; = LEFT JOIN + WHERE 右表主键 IS NULL```\n</code></pre>"
  },
  {
    id: "mysql-user-privilege",
    title: "用户权限",
    date: "2026-09-04",
    tag: "MySQL",
    tagClass: "t-mysql",
    summary: "先问：为什么要建用户？",
    content: "<p>先问：为什么要建用户？</p>\n<p>你现在一直用 <strong>root</strong> 干活。想想风险：root 能删任何库、改任何权限——<strong>给同事开账号用 root？给应用连数据库用 root？</strong> 出事就是全库遭殃。</p>\n<blockquote>\n<p><strong>面试考点（必背）</strong>：<strong>最小权限原则</strong>——每个人/每个应用只给够用的权限。同事只要查数，就给只读；应用只要读写业务库，就别给它删表的权限。root 只留给自己。</p>\n</blockquote>\n<p><strong>语法：两兄弟 CREATE USER + GRANT</strong></p>\n<pre><code class=\"language-sql\">-- ① 建用户：'用户名'@'主机'，密码 'xxx'\nCREATE USER 'reader'@'localhost' IDENTIFIED BY '123456';\n\n-- ② 授权：GRANT 权限 ON 哪个库.哪张表 TO 哪个用户\nGRANT SELECT ON school.* TO 'reader'@'localhost';  --SELEC：权限，school.*：库，表（`*.*`=所有库）\n</code></pre>\n<h3>练习：建一个\"只读小助手\"账号</h3>\n<pre><code class=\"language-sql\">-- ① 建只读账号\nCREATE USER 'reader'@'localhost' IDENTIFIED BY '123456';\n\n-- ② 只给 school 库的 SELECT（查）权限——别的啥都不给\nGRANT SELECT ON school.* TO 'reader'@'localhost';\n\n-- ③ 检查授权结果\nSHOW GRANTS FOR 'reader'@'localhost';\n</code></pre>\n<p>然后<strong>新开一个终端窗口</strong>（当前 root 窗口别关），验证：</p>\n<pre><code class=\"language-sql\">mysql -u reader -p123456        # 用 reader 登录（-p 后直接跟密码）\nUSE school;\nSELECT * FROM student;            -- 应该成功（有 SELECT 权限）\nINSERT INTO student (name, age, score, class) VALUES ('测试', 1, 1, '一班');   -- 应该报权限错误！\n</code></pre>\n<p><strong>预期</strong>：SELECT 成功，INSERT 报 <code>ERROR 1142 (42000): SELECT command denied...</code>（或类似权限拒绝）——看到报错就说明<strong>权限起作用了</strong>！</p>\n<h3>🔐 权限四兄弟补全（面试闭环）</h3>\n<table>\n<thead>\n<tr>\n<th style=\"text-align: center;\">命令</th>\n<th>作用</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td style=\"text-align: center;\">GRANT SELECT ON school.* TO 'reader'@'localhost</td>\n<td>给权限</td>\n</tr>\n<tr>\n<td style=\"text-align: center;\">REVOKE SELECT ON school.* FROM 'reader'@'localhost</td>\n<td>收权限</td>\n</tr>\n<tr>\n<td style=\"text-align: center;\">ALTER USER 'reader'@'localhost' IDENTIFIED BY '新密码'</td>\n<td>改密码</td>\n</tr>\n<tr>\n<td style=\"text-align: center;\">DROP USER 'reader'@'localhost</td>\n<td>删用户（离职/不用了）</td>\n</tr>\n</tbody>\n</table>\n<p>记住存在即可，重点是 <strong>GRANT 给权限</strong> 和 <strong>最小权限</strong> 两个概念。</p>"
  },
  {
    id: "mysql-mysqldump",
    title: "mysqldump 备份恢复（面试必问：\"数据库怎么备份\"）",
    date: "2026-09-04",
    tag: "MySQL",
    tagClass: "t-mysql",
    summary: "实操：备份 → 删表 → 恢复（完整演练）",
    content: "<p>实操：备份 → 删表 → 恢复（完整演练）</p>\n<p><strong>备份 school 整个库（shell 里执行）：</strong></p>\n<p><code>sudo mysqldump -u root school &gt; ~/school_backup.sql</code></p>\n<p>语法拆解：<code>mysqldump</code>（导出工具）+ <code>-u root</code>（用 root）+ <code>school</code>（导哪个库）+ <code>&gt;</code>（重定向写入文件——你学的 Linux 重定向用上了！）</p>\n<p><strong>确认备份文件生成了（顺便复习 Linux 命令）：</strong></p>\n<pre><code>ls -lh ~/school_backup.sql    # 看文件多大\nhead -20 ~/school_backup.sql  # 看文件内容（里面是 SQL 语句！）\n</code></pre>\n<p><strong>用备份恢复：</strong></p>\n<pre><code># 把备份文件&quot;重放&quot;进 school 库\nsudo mysql school &lt; ~/school_backup.sql\n\n# 验证：student 表回来了吗？\nsudo mysql -e &quot;SELECT * FROM school.student;&quot;\n</code></pre>\n<p>📦 mysqldump 小结</p>\n<pre><code>备份：mysqldump -u root 库名 &gt; 备份.sql    （&gt; 导出）\n恢复：mysql 库名 &lt; 备份.sql                （&lt; 导入）\n</code></pre>\n<blockquote>\n<p><strong>面试考点</strong>：备份策略怎么答？初级版：<strong>每天定时 mysqldump 全库导出</strong>（配合 crontab，你 W4 会学）。高级版加一句：备份文件要<strong>放到另一台机器/磁盘</strong>（防止机器挂了备份也没了）。</p>\n</blockquote>"
  },
  {
    id: "mysql-charset",
    title: "乱码处理",
    date: "2026-09-04",
    tag: "MySQL",
    tagClass: "t-mysql",
    summary: "你看到的 ??? 或 æ±‰å—，本质是：写入时的字符集 ≠ 读取时的字符集——像两个人用不同的密码本对话，全对不上。",
    content: "<p>你看到的 <code>???</code> 或 <code>æ±‰å—</code>，本质是：<strong>写入时的字符集 ≠ 读取时的字符集</strong>——像两个人用不同的密码本对话，全对不上。</p>\n<p>现场演示：乱码是怎么来的</p>\n<pre><code class=\"language-sql\">SET NAMES latin1;        -- ① 把客户端密码本换成老外的 latin1\nSELECT * FROM student;   -- ② 中文应该变乱码了！（数据没坏，只是读错码本）\nSET NAMES utf8mb4;       -- ③ 换回 utf8mb4\nSELECT * FROM student;   -- ④ 中文回来了\n</code></pre>"
  }
];
